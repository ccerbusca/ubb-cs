def main(): void {
    var s: Integer = 0;
    val 3l: Array[Integer] = 1..100;

    for (var i = 1; i < l.length i += 1) {  
        s += l(i);
    }
    println(`Sum of numbers up to ${n} is ${s});
}